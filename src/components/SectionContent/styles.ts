import { Box, styled } from '@mui/material';

export const Container = styled(Box)(() => ({
  width: '100%',
  paddingLeft: '150px',
  paddingRight: '150px',
  paddingTop: '96px',
  paddingBottom: '96px',
}));
